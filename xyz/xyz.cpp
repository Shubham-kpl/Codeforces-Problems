#include <bits/stdc++.h>
using namespace std;

#define r 10
#define c 20

int main()
{
    // // Check how many significant digits
    // cout << 32 - __builtin_clz(9) << endl;

    // vector<int> v(2);
    // cout << (*v.begin() == *(&v[0])) << endl;

    // // vector has a front() function too (15/5/24)
    // vector<int> v = {1, 2, 3, 4, 5};
    // cout << (v.front()) << endl;

    // // a peculiar problem with float (and double) (15/5/24)
    // double a = 1000000;
    // int b = 100000000;
    // cout << a << " " << b << endl;

    // // >> (15/5/24)
    // cout << (2 + 3 >> 1) << endl;

    int x = 100'100'100;
    cout << x << '\n';
}