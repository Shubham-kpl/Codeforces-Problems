#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        if( n % 2 == 1) {
            string ansStr = "";
            for(int i = 0; i < n; i++) {
                if(i %2 == 0) ansStr += 'a';
                else ansStr += 'b';
            }
            
        } 
    }
}